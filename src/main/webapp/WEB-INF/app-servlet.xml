<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:mvc="http://www.springframework.org/schema/mvc"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- XML 설정 파일에서는 페이지 컨트롤러 객체만 생성한다 -->
    <context:component-scan base-package="leadme"/>
    <mvc:annotation-driven enable-matrix-variables="true"/>
    <!-- WebConfig 클래스에 설정 했기 때문에 다음 태그는 제거한다 -->
    <!-- <mvc:annotation-driven enable-matrix-variables="true"/> -->
    
    <!-- Servlet3.0에서 제공하는 멀티파트 처리기를 사용하지 않고
            (서블릿 컨테이너가 multipart/form-data를 직접처리하는 것이 아니라
            - Spring 프레임워크에서 직접 처리하고 싶다면,
            - 다음 객체를 직접 등록하라
            multipart/form-data를 처리하는 객체 등록 
          - 다만 
          - 다음 객체를 등록하지 않아도 된다
    -->
    <!-- 
    <bean id="multipartResolver" 
          class="org.springframework.web.multipart.commons.CommonsMultipartResolver"> 
        <property name="maxUploadSize" value="2000000" /> 
    </bean>
     -->

    <!-- 기존의 ViewResolver를 다른것 으로 교체한다
        - 이 ViewResolver는 request handler가 리턴한 경로의
        - 앞, 뒤에 공통 경로를 추가하여 JSP파일을 찾아 실행한다
    -->
    <bean id="viewResolver" 
          class="org.springframework.web.servlet.view.InternalResourceViewResolver">
	    <property name="viewClass" value="org.springframework.web.servlet.view.JstlView"/>
	    <property name="prefix" value="/WEB-INF/jsp/"/>
	    <property name="suffix" value=".jsp"/>
	</bean>
	
	
	<bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource"> 
    <property name="basenames"> 
        <list> <!-- 메세지 파일의 위치를 지정합니다. message_언어.properties 파일을 찾습니다. --> 
            <value>/WEB-INF/messages/message</value> 
        </list> 
    </property> <!-- 파일의 기본 인코딩을 지정합니다. --> 
    <property name="defaultEncoding" value="UTF-8" /> <!-- properties 파일이 변경되었는지 확인하는 주기를 지정합니다. 60초 간격으로 지정했습니다. --> 
    <property name="cacheSeconds" value="60" /> </bean> <!-- 언어 정보를 세션에 저장하여 사용합니다. --> 
    <bean id="localeResolver" class="org.springframework.web.servlet.i18n.SessionLocaleResolver" />
    
    <mvc:interceptors> 
        <bean id="localeChangeInterceptor" class="org.springframework.web.servlet.i18n.LocaleChangeInterceptor"> 
            <property name="paramName" value="lang" /> 
        </bean>
        <mvc:interceptor>
            <mvc:mapping path="/**/myInfo"/>
            <bean class="leadme.web.intercepter.GuideInterCepter"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/**/payment"/>
            <mvc:mapping path="/**/enroll/**/"/>
            <!-- <mvc:mapping path="/**/myInfo"/> -->
            <bean class="leadme.web.intercepter.AuthInterCepter"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/**/login"/>
            <bean class="leadme.web.intercepter.LoginInterCepter"/>
        </mvc:interceptor>
    </mvc:interceptors>

</beans>















